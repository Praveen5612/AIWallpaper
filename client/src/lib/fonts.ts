// This file ensures the custom fonts are properly loaded

// Import fonts from Google Fonts via CDN - already in index.html
// Inter (300, 400, 500, 600, 700)
// Orbitron (400, 500, 600, 700)
// Space Mono (400)

// Apply the font to the body
document.body.classList.add('font-sans');
